# FastAPI Scaffold

This repository provides a minimal FastAPI application.

## Running

Install dependencies and start the server with:

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```
